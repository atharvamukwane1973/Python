# Type Content here...
d=int(input())
m=int(input())
y=int(input())
leap =(y % 400 == 0) or (y % 4 == 0 and y % 100 != 0)
if m<1 or m > 12:
	print("Invalid Date")
elif m in [1,3,5,7,8,10,12]:
	if d<1 or d>31:
		print("Invalid Date")
	elif d==31:
		if m==12:
			print(f"01-01-{y+1}")
		else:
			print(f"01-{m+1:02d}-{y}")
	else:
		print(f"{d+1:02d}-{m:02d}-{y}")
elif m in [4,6,9,11]:
	if d<1 or d>30:
		print("Invalid Date")
	elif d ==30:
		print(f"01-{m+1:02d}-{y}")
	else:
		print(f"{d+1:02d}-{m:02d}-{y}")
elif m==2:
	if leap:
		if d<1 or d>29:
			print("Invalid Date")
		elif d == 29:
			print(f"01-03-{y}")
		else:
			print(f"{d+1:02d}-02-{y}")
	else:
		if d<1 or d>28:
			print("Invalid Date")
		elif d==28:
			print(f"01-03-{y}")
		else:
			print(f"{d+1:02d}-02-{y}")
